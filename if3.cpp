#include <stdio.h>
int main(){
	char a, b;
	printf("Are you doing fine? Press 'Y for Yes and 'N' for No: ", a);
	scanf("%c", &a);
	
		if(a=='Y')
			printf("Good to hear!", a);
		if(a=='N')
			printf("Not all days are bad.", a);
		}
